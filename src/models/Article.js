import React from "react";

export default function Article({ id, title, description }) {
  return (
    <div>
      <p>{article.id}</p>
      <p>{article.title}</p>
      <p>{article.description}</p>
    </div>
  );
}
